export interface movieType {
  title: string;
  thumbnail: string;
  videoURI: string;
  votes: string;
  like: string;
  language: string;
  genres: Array<string>;
  displayDate: Array<string>;
  fullDate: string;
}
